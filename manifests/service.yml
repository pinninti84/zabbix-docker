apiVersion: v1
kind: Service
metadata:
    name: "akszabbix-2dc5"
    labels:
        app: "akszabbix-2dc5"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akszabbix-2dc5"